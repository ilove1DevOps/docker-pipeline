pipeline {
    agent any {
        stages {
        stage('Build') {
            steps {
                // Perform test steps here
                sh 'docker build -t piyushdhir121:i1 .'
            }
        }
    }
}
}
